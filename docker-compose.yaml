version: '3:8'
services:
  db:
   image: postgres
   volumes:
     - pgdata:/var/lib/postgresql/data
   ports:
     - "5555:5555"
   environment:
    - POSTGRES_PASSWORD:bismillah
    - POSTGRES_DB:irfan-db
    - POSTGRES_USER:postgres
volumes:
  pgdata:

