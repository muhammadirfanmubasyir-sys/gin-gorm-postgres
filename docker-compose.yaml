version: '3:8'
services:
  db:
   image: postgres
   volumes:
     - pgdata:/var/lib/postgresql/data
   ports:
     - "5432:5555"
   environment:
    - POSTGRES_PASSWORD:password
    - POSTGRES_DB:postgres
    - POSTGRES_USER:postgres
volumes:
  pgdata:

